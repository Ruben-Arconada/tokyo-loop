# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Claude Code session/tooling artifacts
.claude/

# Working material kept out of git on purpose (AGENTS.md says so, but a note in
# a document is not a guard: until now the only thing standing between these
# 10 MB and the repo was remembering never to run `git add -A`).
# Anchored with a leading slash: bare `assets/` would also swallow a future
# src/assets or public/assets without ever saying so.
/assets/
/experiments/
